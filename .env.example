# Default ports used by docker-compose.yml
FRONTEND_PORT=5173
BACKEND_PORT=8000

# Example of how the frontend can reach the backend while running locally
API_BASE_URL=http://backend:8000
VITE_API_BASE_URL=https://backend:8000
